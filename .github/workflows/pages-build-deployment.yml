# This file disables the automatic GitHub Pages Jekyll workflow
# We use our custom deploy.yml workflow instead

name: pages-build-deployment

on:
  push:
    branches:
      - main

jobs:
  skip:
    runs-on: ubuntu-latest
    steps:
      - name: Skip automatic Pages deployment
        run: echo "Using custom deploy.yml workflow instead"
